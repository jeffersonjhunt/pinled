dependencies:
  idf: '>=5.5'
  # The in-house driver (github.com/jeffersonjhunt/esp32-idf-ws2812b),
  # born when its predecessor was caught transmitting every colour byte
  # bit-reversed. Pinned to a commit, same policy as before: the wire
  # contract is proven per-version on the bench, not assumed.
  esp32-idf-ws2812b:
    git: "https://github.com/jeffersonjhunt/esp32-idf-ws2812b.git"
    version: "b0eea3feef42"
